<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.0.xsd"
	default-autowire="no">
	<!-- 数据库联接配置域 -->

	<bean id="fmkdataSource" class="oracle.jdbc.pool.OracleDataSource">
    	<property name="maxStatements" value="20" />
    	<property name="connectionCachingEnabled" value="true" />
        <property name="user" value="${database.user}" />
        <property name="password" value="${database.password}" />
        <property name="URL" value="jdbc:oracle:thin:@//${database.host}:${database.port}/${database.service_name}" />
    </bean>
  	<!-- 
    <bean id="fmkdataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">	
	    <property name="driverClassName" value="com.mysql.jdbc.Driver"/>
        <property name="url" value="jdbc:mysql://${database.host}:${database.port}/${database.service_name}" />
        <property name="username" value="${database.user}" />
        <property name="password" value="${database.password}" />           
    </bean >
    -->
</beans>